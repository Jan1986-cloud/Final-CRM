{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "rootDir": "frontend"
  },
  "deploy": {
    "startCommand": "npx serve -s dist -p $PORT",
    "healthcheckPath": "/"
  }
}